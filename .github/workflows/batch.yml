name: 头条内容早午晚批次发布

permissions:
  contents: write

# 纯 GitHub Actions 调度（无外部依赖）：
#   每个批次 3 个 cron，从目标时间开始每隔 ~10-20 分钟触发一次
#   第一批成功执行后，后续通过幂等保护自动跳过
#   += 晨读增加额外预触发窗口 (UTC 23:55 = CST 07:55)，与 00:xx 不同时间窗，
#      规避 GH cron 某时段集体延迟导致所有触发点同时错过的问题。
#
#   晨读 (目标 08:00 CST, UTC 00:00):
#     07:55 (预触发, UTC 23:55) / 08:07 / 08:17 / 08:37 CST
#   午间 (目标 12:00 CST, UTC 04:00):
#     12:07 / 12:17 / 12:37 CST
#   晚间 (目标 17:30 CST, UTC 09:30):
#     17:17 (预触发, UTC 09:17) / 17:37 / 17:47 / 18:07 CST
#
#   批次检测: schedule 事件从 github.event.schedule 解析 cron 表达式的 UTC hour
#   (0/23→morning, 4→noon, 9/10→evening)，不依赖当前时间，彻底解决 cron 延迟导致的批次误判。
#   workflow_dispatch 时直接使用 inputs.batch 参数。
on:
  schedule:
    # --- 晨读预触发 (UTC 23:55 = CST 07:55，独立时间窗降低集体延迟风险) ---
    - cron: '55 23 * * *'
    # --- 晨读 (UTC 0 = CST 8) ---
    - cron: '7 0 * * *'
    - cron: '17 0 * * *'
    - cron: '37 0 * * *'
    # --- 午间 (UTC 4 = CST 12) ---
    - cron: '7 4 * * *'
    - cron: '17 4 * * *'
    - cron: '37 4 * * *'
    # --- 晚间 (UTC 9 = CST 17) ---
    # 预触发 (UTC 09:17 = CST 17:17，低于目标时间延迟风险)
    - cron: '17 9 * * *'
    - cron: '37 9 * * *'
    - cron: '47 9 * * *'
    - cron: '7 10 * * *'
  workflow_dispatch:
    inputs:
      batch:
        description: '批次 (morning/noon/evening，留空自动判断)'
        required: false
        type: choice
        options:
          - morning
          - noon
          - evening
      date:
        description: '日期 (YYYY-MM-DD，默认今天北京时间)'
        required: false
        type: string
        default: ''

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    permissions:
      contents: write

    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 配置 Git 推送凭据
        run: |
          # 显式把 GITHUB_TOKEN 注入 remote，确保 git push 走令牌鉴权
          git remote set-url origin "https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: 设置 Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: 安装依赖
        run: |
          pip install -r requirements.txt
          playwright install chromium
          playwright install-deps chromium

      - name: 静态分析 — 检查无未定义的函数引用
        run: |
          python3 tests/test_no_dead_references.py

      - name: 写入头条号登录状态
        run: |
          mkdir -p /tmp/auth
          echo "${{ secrets.TOUTIAO_AUTH_GZ }}" | base64 -d | python3 -c "import sys,gzip; sys.stdout.buffer.write(gzip.decompress(sys.stdin.buffer.read()))" > /tmp/auth/toutiao_auth.json

      - name: 确定批次和日期
        id: params
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Date
          if [ -z "${{ inputs.date }}" ]; then
            TODAY=$(TZ=Asia/Shanghai date +%Y-%m-%d)
          else
            TODAY="${{ inputs.date }}"
          fi
          echo "today=$TODAY" >> $GITHUB_OUTPUT

          # Batch determination
          # For schedule events: parse github.event.schedule to get the INTENDED
          # batch, not the current execution time. This is critical because GitHub
          # cron delays of 5+ hours will cause UTC-hour-window detection to misidentify
          # the batch (e.g., morning cron delayed to UTC 4 → detected as noon).
          # Falls back to UTC window detection if schedule parsing fails.
          if [ "${{ github.event_name }}" = "schedule" ]; then
            SCHEDULE_HOUR=$(echo "${{ github.event.schedule }}" | awk '{print $2}')
            UTC_HOUR=$(date -u +%H)
            CST_HOUR=$(TZ=Asia/Shanghai date +%H)
            case "$SCHEDULE_HOUR" in
              0|23) BATCH="morning" ;;
              4)    BATCH="noon" ;;
              9|10) BATCH="evening" ;;
              *)
                # Fallback: UTC hour window (±2h tolerance)
                if [ "$UTC_HOUR" -ge 0 ] && [ "$UTC_HOUR" -le 2 ]; then BATCH="morning"
                elif [ "$UTC_HOUR" -ge 22 ] && [ "$UTC_HOUR" -le 23 ]; then BATCH="morning"
                elif [ "$UTC_HOUR" -ge 4 ] && [ "$UTC_HOUR" -le 6 ]; then BATCH="noon"
                elif [ "$UTC_HOUR" -ge 9 ] && [ "$UTC_HOUR" -le 12 ]; then BATCH="evening"
                else
                  if [ "$CST_HOUR" -lt 10 ]; then BATCH="morning"
                  elif [ "$CST_HOUR" -lt 15 ]; then BATCH="noon"
                  else BATCH="evening"
                  fi
                fi
                ;;
            esac
            echo "batch=$BATCH" >> $GITHUB_OUTPUT
            echo "运行: $TODAY $BATCH 批次 (schedule trigger, UTC=$UTC_HOUR, CST=$CST_HOUR)"
          elif [ -n "${{ inputs.batch }}" ]; then
            BATCH="${{ inputs.batch }}"
            echo "batch=$BATCH" >> $GITHUB_OUTPUT
            echo "运行: $TODAY $BATCH 批次 (手动指定)"
          else
            HOUR=$(TZ=Asia/Shanghai date +%H)
            if [ "$HOUR" -lt 10 ]; then
              BATCH="morning"
            elif [ "$HOUR" -lt 15 ]; then
              BATCH="noon"
            else
              BATCH="evening"
            fi
            echo "batch=$BATCH" >> $GITHUB_OUTPUT
            echo "运行: $TODAY $BATCH 批次 (auto-detected, CST=$HOUR:00)"
          fi

          # 时区校验 — 仅对 auto-detect（无手动指定批次）执行
          if [ "${{ github.event_name }}" != "schedule" ] && [ -z "${{ inputs.batch }}" ]; then
            HOUR=$(TZ=Asia/Shanghai date +%H)
            valid=true
            case "$BATCH" in
              morning)
                if [ "$HOUR" -lt 6 ] || [ "$HOUR" -gt 10 ]; then
                  echo "❌ 时区错误！morning 批次期望北京时间 06:00-10:00，当前 ${HOUR}:00"
                  valid=false
                fi ;;
              noon)
                if [ "$HOUR" -lt 10 ] || [ "$HOUR" -gt 15 ]; then
                  echo "❌ 时区错误！noon 批次期望北京时间 10:00-15:00，当前 ${HOUR}:00"
                  valid=false
                fi ;;
              evening)
                if [ "$HOUR" -lt 15 ] || [ "$HOUR" -gt 22 ]; then
                  echo "❌ 时区错误！evening 批次期望北京时间 15:00-22:00，当前 ${HOUR}:00"
                  valid=false
                fi ;;
            esac
            if [ "$valid" = false ]; then
              echo "🔧 如果是手动触发，请指定正确的 batch 参数"
              exit 1
            fi
            echo "✅ 时区校验通过"
          fi

          # 幂等保护：通过 GitHub API 读取最新 metadata（比本地文件更可靠）
          echo "   检查批次是否已执行..."
          META_API="repos/${{ github.repository }}/contents/output/${TODAY}/metadata.json?ref=main"
          COMPLETED=$(gh api "$META_API" --jq '.content | @base64d | fromjson | .batches_completed | join(",")' 2>/dev/null) || true
          if echo "$COMPLETED" | grep -q "$BATCH"; then
            echo "⏭️  批次 ${BATCH} 今日已完成，跳过"
            echo "skip=true" >> $GITHUB_OUTPUT
            exit 0
          fi
          echo "skip=false" >> $GITHUB_OUTPUT
          echo "   批次 ${BATCH} 尚未执行，继续"

      - name: 时区诊断
        if: always()
        run: |
          echo "╔══════════════════════════════════════╗"
          echo "║  🕐 时区诊断                         ║"
          echo "╠══════════════════════════════════════╣"
          printf "║  %-20s %s ║\n" "UTC:" "$(date -u '+%Y-%m-%d %H:%M:%S')"
          printf "║  %-20s %s ║\n" "CST (Asia/Shanghai):" "$(TZ=Asia/Shanghai date '+%Y-%m-%d %H:%M:%S')"
          printf "║  %-20s %s ║\n" "GitHub event:" "${{ github.event_name }}"
          printf "║  %-20s %s ║\n" "Detected batch:" "${{ steps.params.outputs.batch }}"
          printf "║  %-20s %s ║\n" "Target date:" "${{ steps.params.outputs.today }}"
          echo "╚══════════════════════════════════════╝"

      - name: 同步已有批次元数据（跨批次去重）
        if: steps.params.outputs.skip != 'true'
        run: |
          # Copy previously pushed metadata from repo to orchestrator's data dir
          # so get_cross_batch_covered() can see earlier batches' articles
          if [ -d "output" ]; then
            mkdir -p "${{ runner.temp }}/output"
            cp -r output/* "${{ runner.temp }}/output/" 2>/dev/null || true
            echo "✅ 已同步历史批次元数据到 OUTPUT_DIR"
          fi

      - name: 生成本批次文章
        id: generate
        if: steps.params.outputs.skip != 'true'
        env:
          HY3_API_KEY: ${{ secrets.HY3_API_KEY }}
          DASHSCOPE_API_KEY: ${{ secrets.DASHSCOPE_API_KEY }}
          UNSPLASH_ACCESS_KEY: ${{ secrets.UNSPLASH_ACCESS_KEY }}
          WORLD_NEWS_API_KEY: ${{ secrets.WORLD_NEWS_API_KEY }}
          TOUTIAO_AUTH_FILE: /tmp/auth/toutiao_auth.json
          OUTPUT_DIR: ${{ runner.temp }}/output
          WXPUSHER_APPTOKEN: ${{ secrets.WXPUSHER_APPTOKEN }}
          WXPUSHER_UID: ${{ secrets.WXPUSHER_UID }}
        run: |
          python orchestrator.py "${{ steps.params.outputs.today }}" --batch="${{ steps.params.outputs.batch }}" --count=1 --defer-completion

      - name: 回写生成结果（跨批次去重）
        if: steps.params.outputs.skip != 'true' && !cancelled()
        env:
          TODAY: ${{ steps.params.outputs.today }}
          BATCH: ${{ steps.params.outputs.batch }}
        run: |
          # Push generated article metadata immediately so later batches
          # (or delayed crons) can see today's already-published topics.
          SRC="${{ runner.temp }}/output/${TODAY}/metadata.json"
          if [ ! -f "$SRC" ]; then
            echo "⚠️ metadata.json 不存在，跳过回写"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          DST="output/${TODAY}/metadata.json"
          mkdir -p "output/${TODAY}"
          python3 -c "
          import json
          from pathlib import Path
          src = Path('${SRC}')
          dst = Path('${DST}')
          new_meta = json.loads(src.read_text())
          if dst.exists():
              old_meta = json.loads(dst.read_text())
              old_batches = set(old_meta.get('batches_completed', []))
              new_batches = set(new_meta.get('batches_completed', []))
              old_articles = {(a.get('title',''), a.get('content_type','')) for a in old_meta.get('articles', [])}
              merged_articles = list(old_meta.get('articles', []))
              for a in new_meta.get('articles', []):
                  key = (a.get('title',''), a.get('content_type',''))
                  if key not in old_articles:
                      merged_articles.append(a)
                      old_articles.add(key)
              new_meta['articles'] = merged_articles
              new_meta['total_articles'] = len(merged_articles)
              new_meta['batches_completed'] = sorted(old_batches | new_batches)
          dst.parent.mkdir(parents=True, exist_ok=True)
          dst.write_text(json.dumps(new_meta, ensure_ascii=False, indent=2))
          " || { echo "⚠️ 元数据合并失败，使用新数据"; cp "$SRC" "$DST"; }
          git add -f "output/${TODAY}/metadata.json" || true
          if git diff --cached --quiet; then
            echo "📝 无变化，跳过提交"
            exit 0
          fi
          git commit -m "chore: ${TODAY} ${BATCH} 生成完成元数据回写"
          for attempt in 1 2 3; do
            if git push origin main; then
              echo "✅ 生成结果已回写仓库"
              break
            fi
            echo "⚠️  推送失败 (attempt $attempt/3)，rebase 后重试..."
            git pull --rebase origin main || true
            /usr/bin/sleep $((attempt * 2))
          done

      - name: 发布到头条号
        if: steps.params.outputs.skip != 'true'
        env:
          HY3_API_KEY: ${{ secrets.HY3_API_KEY }}
          DASHSCOPE_API_KEY: ${{ secrets.DASHSCOPE_API_KEY }}
          UNSPLASH_ACCESS_KEY: ${{ secrets.UNSPLASH_ACCESS_KEY }}
          TOUTIAO_AUTH_FILE: /tmp/auth/toutiao_auth.json
          OUTPUT_DIR: ${{ runner.temp }}/output
        run: |
          python publisher.py "${{ steps.params.outputs.today }}" --headless --batch="${{ steps.params.outputs.batch }}"

      - name: 生成并发布对应微头条
        if: steps.params.outputs.skip != 'true'
        env:
          DASHSCOPE_API_KEY: ${{ secrets.DASHSCOPE_API_KEY }}
          TOUTIAO_AUTH_FILE: /tmp/auth/toutiao_auth.json
          OUTPUT_DIR: ${{ runner.temp }}/output
        run: |
          python micro_publisher.py "${{ steps.params.outputs.today }}" --batch="${{ steps.params.outputs.batch }}"

      - name: 回写批次状态（幂等保护）
        if: success() && steps.params.outputs.skip != 'true'
        env:
          TODAY: ${{ steps.params.outputs.today }}
          BATCH: ${{ steps.params.outputs.batch }}
        run: |
          SRC="${{ runner.temp }}/output/${TODAY}/metadata.json"

          if [ ! -f "$SRC" ]; then
            echo "⚠️ metadata.json 不存在，跳过回写"
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          DST="output/${TODAY}/metadata.json"
          mkdir -p "output/${TODAY}"

          # 合并 batches_completed，避免覆盖之前批次的状态
          # BUGFIX: 从 dst（step 11 已合并后的元数据）读取，而非 src（仅当前批次）
          # 否则会覆盖 step 11 合并好的跨批次文章列表
          python3 -c "
          import json, os
          from pathlib import Path

          src = Path('${SRC}')
          dst = Path('${DST}')

          if dst.exists():
              # 使用 step 11 已合并的元数据（包含所有批次文章）
              new_meta = json.loads(dst.read_text())
              # 仅从 src 合并当前批次的完成状态
              if src.exists():
                  src_meta = json.loads(src.read_text())
                  old_batches = set(new_meta.get('batches_completed', []))
                  new_batches = set(src_meta.get('batches_completed', []))
                  new_meta['batches_completed'] = sorted(old_batches | new_batches)
          else:
              # step 11 可能未执行（如 metadata.json 不存在），直接用 src
              new_meta = json.loads(src.read_text())

          current_batch = os.environ.get('BATCH', '')
          completed = set(new_meta.get('batches_completed', []))
          if current_batch:
              completed.add(current_batch)
          new_meta['batches_completed'] = sorted(completed)

          dst.parent.mkdir(parents=True, exist_ok=True)
          dst.write_text(json.dumps(new_meta, ensure_ascii=False, indent=2))
          print(f'merged batches: {new_meta[\"batches_completed\"]}')
          " || { echo "⚠️ 元数据合并失败，使用新数据"; cp "$SRC" "$DST"; }

          git add -f "output/${TODAY}/metadata.json" || true

          if git diff --cached --quiet; then
            echo "📝 无变化，跳过提交"
            exit 0
          fi

          git commit -m "chore: ${TODAY} ${BATCH} 批次完成"

          # 重试推送：处理多批次并发写入时的 push 冲突
          # 注意：git pull --rebase 必须带 || true，否则其失败会以 128 顶出整步
          for attempt in 1 2 3; do
            if git push origin main; then
              echo "✅ 批次状态已回写仓库"
              break
            fi
            echo "⚠️  推送失败 (attempt $attempt/3)，rebase 后重试..."
            git pull --rebase origin main || true
            /usr/bin/sleep $((attempt * 2))
          done
          echo "ℹ️ 若上方出现推送失败：文章已发布成功，仅仓库元数据回写未成功（下次运行可自动补齐）"
      - name: 上传产物
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: articles-${{ steps.params.outputs.batch }}-${{ steps.params.outputs.today }}
          path: ${{ runner.temp }}/output/${{ steps.params.outputs.today }}/

      - name: 推送诊断（排错用，始终运行）
        if: always()
        run: |
          echo "=== git remote -v ==="
          git remote -v
          echo "=== 最近一次提交 ==="
          git log -1 --oneline 2>/dev/null || true
          echo "=== 排错提示 ==="
          echo "若文章已成功发布到头条号，但本运行仍因 git push 失败而告警："
          echo "请检查仓库 Settings → Branches → main 是否开启了"
          echo "'Require a pull request before merging'（该保护会禁止 GITHUB_TOKEN 直接推送）。"
          echo "如开启，请关闭该保护，或为 GITHUB_TOKEN 添加绕过规则。"

      - name: 失败通知
        if: failure()
        env:
          WXPUSHER_APPTOKEN: ${{ secrets.WXPUSHER_APPTOKEN }}
          WXPUSHER_UID: ${{ secrets.WXPUSHER_UID }}
        run: |
          python -c "
          import requests
          requests.post('https://wxpusher.zjiecode.com/api/send/message',
            json={'appToken':'$WXPUSHER_APPTOKEN','content':'⚠️ ${{ steps.params.outputs.batch }} 批次发布失败\n日期: ${{ steps.params.outputs.today }}\n查看: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}','contentType':1,'uids':['$WXPUSHER_UID']})
          "
